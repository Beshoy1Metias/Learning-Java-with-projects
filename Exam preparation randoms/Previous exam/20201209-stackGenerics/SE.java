public class SE <T extends Comparable> extends S <T>{	private Object[] arr;	public SE()	{		makeEmpty();	}	public Stack clone()	{		arr = toArray();		Stack<T> stack = new SE();				for(int i=arr.length-1; i>=0;i--)		{			stack.push((T)arr[i]);		}		return stack;			}	public Stack reverse()	{		Stack<T> stack = new SE();				for(int i=0; i<arr.length;i++)		{			stack.push((T)arr[i]);		}		return stack;	}	public Object[] toArray()	{		int Size = size();		Object[] tmp = new Object[Size];		for(int i=0; i<Size; i++)		{			tmp[i]=(T)super.pop();		}		for(int i=Size-1; i>=0; i--)		{			super.push((T)tmp[i]);		}		return tmp;	}	public Object[] toSortedArray() //34451	{		sort(arr);		return arr;	}	private void sort(Object[] a)	{		for(int i =1; i<a.length;i++)		{			int j=i-1;			T current = (T)a[i];			while(current.compareTo(a[j])<0 && j>0)			{				a[j+1]=a[j];				j--;			}			a[j+1]=current;		}	}}